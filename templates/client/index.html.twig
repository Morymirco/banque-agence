{% extends 'template.html.twig' %}

{% block title %}liste des clients{% endblock %}
{% block link %}
<li class="nav-item">
                  <a href="{{path('app_client_index')}}">
                     client
                  </a>
                </li>
{% endblock %}
{% block link2 %}
  <li class="nav-item">
                  <a href="{{path('app_client_index')}}">liste</a>
                </li>
{% endblock %}
{% block content %}
 {% for label, messages in app.flashes %}
        <div class="alert alert-{{ label }} alert-dismissible fade show p-4 mb-4" role="alert" style="font-size: 0.875rem;">
            {% for message in messages %}
                <p class="mb-0">{{ message }}</p>
            
            {% endfor %}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
  {% endfor %}
  <div class="card">
  <h1 class="mb-3"></h1>
<div class="card-header">
                    <div class="d-flex align-items-center">
                      <h4 class="card-title">liste des Clients</h4>
                     
                      <button
                        class="btn btn-primary btn-round ms-auto"
                        data-bs-toggle="modal"
                        data-bs-target="#addRowModal"
                      >
                        <i class="fa fa-plus"></i>
                          <a href="{{ path('app_client_new') }}" class="text-white">
                      Ajouter un client
                      </a>
                      <button
                        class="btn btn-label-info btn-round mx-2"
                        data-bs-toggle="modal"
                        data-bs-target="#addRowModal"
                      >
                        <i class="fas fa-download"></i>
                          <a href="{{ path('clients2_pdf') }}" >
                    Telecharger
                      </a>
                      </button>     
                    </div>

                  </div>

                    <table class="table table-striped table-hover">
               <thead>
            <tr>
                <th>Identifiant</th>
                <th>Prenom</th>
                <th>Nom</th>
                <th>Sexe</th>
                <th>Agence</th>
                <th>Numero</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
 {% for client in clients %}
             <tr>
          <td>
            {{ client.id  }}
          </td>
          <td >
           {{ client.prenomClient }}
          </td>
          <td >
        {{ client.nomClient }}
          </td>
          <td >
        {{ client.sexeClient}}
          </td>
          <td >
        {{ client.Agence}}
          </td>
          <td >
        {{ client.numeroClient}}
          </td>
          <td >
         
             <a href="{{ path('app_client_show', {'id': client.id}) }}" class="btn btn-info"><i class="fa fa-eye"></i></a>
             <a href="{{ path('app_client_edit', {'id': client.id}) }}" class="btn btn-success"><i class="fa fa-edit  mx-2"></i></a>
             {# <a href="{{ path('app_client_delete', {'id': client.id}) }}"><i class="fa fa-trash text-danger"></i></a> #}
         
          </td>
        
              
                </td>
            </tr>
         
                  
        {% else %}
            <tr>
                <td colspan="7">Aucun client n'a ete trouvé !!</td>
            </tr>

            
        {% endfor %}
        </tbody>
    </table> 


<div class="navigation">
        {{ knp_pagination_render(clients,'pagination.html.twig') }}
    </div>

  </div>
</div>
  
    {# <a href="{{ path('app_client_new') }}">Create new</a> #}
  

{% endblock %}