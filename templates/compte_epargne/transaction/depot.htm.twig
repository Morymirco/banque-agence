{% extends 'base.html.twig' %}

{% block title %}Dépôt{% endblock %}

{% block body %}
    <h1>Dépôt sur le compte épargne</h1>

    {{ form_start(form) }}
        {{ form_row(form.montant) }}
        <button class="btn btn-primary">Déposer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_compte_epargne_show', {'id': compte_epargne.id}) }}" class="btn btn-secondary">Retour au compte</a>
{% endblock %}
