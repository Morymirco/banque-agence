{% extends 'template.html.twig' %}

{% block title %}Modifier L'Agence{% endblock %}

{% block content %}
    <h1>Modifier l'Agence</h1>

   <div class="flex ">

    {{ include('agence/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('agence/_delete_form.html.twig') }}
</div>
    <a href="{{ path('app_agence_index') }}" class="btn btn-dark btn-sm w-30 mb-3 mt-5">Retour en arriere</a>
{% endblock %}
